# macOS 검증 기록

## 2026-09-13 갱신 — 결함 2건 수정, 실기 재검증 대기

지난 실행에서 실패로 남은 9·16번의 원인을 코드에서 확정하고 수정했다. 자동 테스트는
통과했으나 **실제 화면 재검증은 아직 하지 않았다.**

- **9번** — [`ui/popup.py`](src/tokentray/ui/popup.py)의 토스트가 `Qt::Tool` 창이었다.
  macOS에서 이는 `hidesOnDeactivate`가 켜진 NSPanel이라 앱이 비활성화되는 순간 OS가
  숨긴다. `WA_MacAlwaysShowToolWindow`를 켜서 해결. 맥은 네이티브 알림 경로가 구조적으로
  죽어 있어 자체 카드가 유일한 채널이므로 이건 알림 전체 손실이었다.
- **16번** — `build_view()`가 번역된 문자열을 view에 구워 넣는데 `set_language()`가
  기존 view를 재렌더만 했다. Controller가 마지막 스냅샷을 보관하고 언어 변경 시
  재빌드하도록 [`app.py`](src/tokentray/app.py) 수정.
- 회귀 테스트 `tests/test_language_switch.py` 추가. 수정 전에는 4건 모두 실패함을 확인했다.
- 별건: `d23c6bd`가 넣은 `test_the_hidden_icon_hint_is_for_windows[win32]`가 맥에서
  실패하고 있었다. `sys.platform`을 `win32`로 바꾸면 stdlib `shutil.which`가 `_winapi`를
  건드려 터진다. 제품 결함은 아니고 테스트가 OS 대신 조회 함수를 스텁하도록 고쳤다.
  즉 **이번 작업 전까지 `dev` HEAD는 맥에서 스위트가 빨간 상태였다.**

### 기준 정정

지난 기록의 기준 커밋 `5efdc2f`는 `dev`의 조상이 아니다. `backup/dev-before-merge`
태그에만 있고, dev는 `272fc0d`에서 다른 경로로 갈라졌다. 그 뒤 검증표가 바뀌었으므로
아래 표의 일부 결과는 **현재 문구를 보증하지 않는다.**

- **8·8a** — 합격 문구가 새로 쓰였다(한도별 잔량 막대, 조회 중 메뉴 비활성화, Windows
  기본값에서 배너 없음). 지난 결과 무효.
- **4·18·27** — 요구사항이 추가됐다(툴팁 `tokentray` 접두, pid 출력, `running yes (pid N)`).
  27의 "통과"는 추가 요구 이전 기록이다.
- **17c → 14d** 로 번호가 이동했다. 지난 "17c 통과"는 14d를 뜻한다.
- **6번**의 M열이 `☐`였으나 본문이 "(Windows/Linux)"로 못박고 있고 맥은 모든 클릭이
  메뉴로 간다. 이번에 `n/a`로 정정했다.
- 신규 기능(로그인 복구, 셋업 마법사 재작업, 새로고침 피드백)에 대응하는 **27a~27f**를
  검증표에 새로 넣었다. 맥 전용으로 `osascript` → Terminal.app 경로와 Automation 권한
  프롬프트가 포함되며, **한 번도 실행된 적이 없다.**

### 2026-09-13에 화면 없이 확인한 것

- `uv run pytest -q` 전체 통과, `uv run ruff check .` 통과.
- `pyinstaller ... packaging/tokentray.spec`로 `.app` 빌드 성공, `packaging/smoke.sh` 통과
  (두 실행 파일 존재, GUI `--version`이 이벤트 루프 대신 답함, `.app` 완전성).
  기존 `dist`는 건드리지 않고 별도 `--distpath`로 뺐다.
- **27번 통과** — doctor가 `running             yes (pid 3243)`을 출력한다.
- **37a번 통과** — 번들 안은 `app bundle  <경로> (ad-hoc)`, 체크아웃은
  `app bundle  none - running from .../.venv/bin/python3`이며 후자에만
  "signed .app bundle" 줄이 붙는다. 양쪽 모두 Notification Centre 안내를 출력한다.

이 셋은 화면을 보지 않고 확인한 것이라 나머지 항목의 상태를 바꾸지 않는다.

### 2026-09-13 실기 검증 — 화면·AX·CLI로 직접 실행

`screencapture`와 Accessibility(System Events), IPC `test-alert`, 합성 커서 입력으로
체크아웃 실행분을 직접 조작해 확인했다. 설정·파일은 모두 백업 후 원복했다.

#### 이번에 새로 찾아 고친 결함 2건

- **토스트 카드 겹침** — 사용자가 화면에서 발견. AX 실측으로 원인을 확정했다.
  `_slot()`이 첫 창을 `area.top() - SHADOW_MARGIN + EDGE_MARGIN` = 9에 놓으려 했는데
  macOS가 메뉴 막대 위로는 창을 못 두게 y=33으로 끌어내렸다. 그런데 아래 카드의
  오프셋은 끌려오기 전 값 9를 기준으로 계산돼서, 카드0 아래끝 230 / 카드1 위끝 216 —
  **14px 겹쳤다.** 요청 좌표를 작업 영역 안으로 제한해 해결. 수정 후 실측은
  카드0 아래끝 230 / 카드1 위끝 240으로 정확히 `GAP` 10px다.
  `SHADOW_MARGIN`이 20에서 40으로 커지면서 드러난 것으로, 산술 자체는 플랫폼 공통이지만
  창을 끌어내리는 쪽이 macOS라 **실질적으로 맥에서만 보인다.**
- **호버해도 8초 카운트다운이 멈추지 않음 (10번)** — Qt는 포인터가 자식 위젯으로
  넘어갈 때 부모에 Leave를 보낸다. 그림자 여백을 지나며 타이머가 멈췄다가 카드 본문에
  올라서는 순간 `leaveEvent`가 다시 8초를 시작해, 커서를 올려둔 채로도 사라졌다.
  포인터가 정말 카드 밖일 때만 재시작하도록 수정. **플랫폼 무관한 결함이다.**
  수정 후 실측: 안 올린 카드는 9.5초에 사라지고 올린 카드는 13초까지 유지, 커서를
  치우자 8.8초 뒤 소멸.

#### 통과

| 항목 | 근거 |
|---|---|
| 1 | 재시작 후 메뉴 막대 아이콘 등장까지 2.06초 (기준 10초). |
| 2 | 색 경계 실측: 51%→green, 50.4%→orange, 21%→orange, 20.4%→red. 체크리스트 문구와 일치. |
| 3 | 32px 렌더 픽셀 검사. 바깥 Claude 80%는 9시까지, 안쪽 Codex 30%는 3시까지 칠해짐 — 12시부터 시계방향 감소 확인. |
| 3a | Codex 미설정 시 안쪽 밴드가 트랙(투명 검정 a38)으로 남고 바깥 링과 합쳐지지 않음. |
| 3b | Codex 0%는 12시에만 `#c64234` 짧은 표시, 3시·9시는 트랙 — 데이터 없음과 구분됨. |
| 5 | 아이콘 클릭으로 메뉴가 열리고 전 항목 한국어(상세 열기·새로고침·알림 1시간 일시정지·알림 테스트·알림 연동 설정…·언어·로그인 시 시작·로그 열기·종료). |
| 7 | 패널 바깥을 클릭하니 패널이 닫힘. |
| 8 | 조회 중 메뉴가 `알림용 사용량 조회 중…`으로 바뀌며 비활성(0.38s~0.86s), 완료 후 복귀. 3연속 요청에도 카드는 2장만. 한도별 잔량 막대와 리셋 시간 표시 확인. |
| 8a | 자체 카드 + OS 알림 시도. `bundle=none`이라 배너는 없고 로그에 `native notification attempted (supported=True)` 기록 남음. |
| 10 | 약 8초 소멸, 호버 시 유지 (위 수정 후). |
| 11 | 카드 본문 클릭으로 카드가 닫히고 상세 창이 열림. 앱이 비활성 상태였을 때의 첫 클릭은 macOS가 활성화용으로 삼키므로 한 번 더 눌러야 한다 — OS 표준 동작. |
| 12 | 쌓임 시 겹침 없음(갭 10px). 위 카드를 닫으니 아래 카드가 빈 위 슬롯(y=33)으로 이동. |
| 14d | `native_notifications=false`에서 auto=위(y=33), bottom-right=아래(카드 아래끝 901 = 917-16), top-right=위. 재시작 없이 전환되고 x는 1083으로 고정. 세 경우 모두 갭 10px. |
| 16 | 트레이 메뉴로 English 전환 시 패널 전체가 영문(`5 hours`·`67% remaining`·`Refills in`·`Burns out in`·`Pace`·`stale`). 한국어 복귀도 정상. **9월 10일 실패 항목이 해소됐다.** |
| 17 | 메뉴·패널·카드·시작 안내의 한글에 글리프 누락이나 잘림 없음. |
| 17a | 영문에서 `5 hours` / `7 days`, `5-Hour Session`이나 `Codex 7d` 섞임 없음. |
| 17b | 패널 제목이 `Claude Code (Max)`·`Codex (Pro Lite)`. |
| 18 | 두 번째 실행이 `tokentray가 이미 실행 중입니다 (pid N) - 상세 패널을 엽니다`를 출력하고 패널을 엶. |
| 19 | `status`가 데이터와 `(from the running app, pid N)`을 출력. |
| 22 | `autostart disable`이 LaunchAgent plist를 실제로 삭제, `enable`이 복구. |
| 23a | `state reset --welcome` 후 강제 새로고침(=저장)을 거쳐도 플래그가 되살아나지 않고, 재시작 시 시작 안내가 다시 표시됨. |
| 26 | `~/.codex/auth.json`을 치우니 Codex만 `Codex 미로그인`으로 표시되고 Claude는 정상 동작. 파일 원복 확인. |
| 27 | doctor가 `running yes (pid N)` 출력. |
| 27f | 패널 새로고침 클릭 시 버튼이 비활성(0.20s)됐다가 완료 후 복귀(0.92s). 연속 클릭이 중복 조회로 이어지지 않음. |
| 37 | Bartender 6 실행 중에도 아이콘이 (1057, 4) 24×24로 화면 안에 있고 클릭으로 메뉴가 열림. 시작 안내에 `Bartender, Ice, Hidden Bar 같은 메뉴 막대 관리 앱을 쓰고 있다면…` 문구 포함. |
| 37a | 번들 안 `app bundle <경로> (ad-hoc)` vs 체크아웃 `app bundle none`, 양쪽 모두 Notification Centre 안내 출력. |

#### 관찰 (결함은 아니지만 기록)

- macOS 시작 안내 본문이 `시스템 트레이에서 백그라운드로 동작해요`다. 바로 다음 줄은
  `메뉴 막대`라고 쓴다. 맥에서는 `메뉴 막대`가 맞는 표현이다.
- `autostart status`가 비활성 상태에서도 plist 경로를 함께 출력한다. 파일은 실제로
  없으므로 경로는 "있었다면 여기" 정도의 뜻이다.
- 26번의 표시는 체크리스트가 말하는 `미설정`이 아니라 `Codex 미로그인`이다.
  새 연결 판정에서는 이쪽이 더 정확하므로 검증표 문구를 맞추는 편이 낫다.

#### 이번에도 못 한 것

- **13** 외부 모니터 없음. **21** 실제 로그아웃 필요. **35** 태그된 릴리스 없음.
- **25 / 32 / 33 / 36** CLI 로그아웃과 키체인 접근 거부는 합의한 범위 밖이라 손대지 않았다.
- **34** Finder 아이콘은 확인 가능하나 Gatekeeper 대화 상자는 격리 속성이 붙은
  배포 파일이 필요하다.
- **14 / 14a / 14b / 14c** 알림 연동 설정 창은 이번에 열지 않았다.
- **22a** 체크아웃에서는 어느 쪽으로 두어도 배너가 없어 판정할 수 없다. `.app`에서 해야 한다.
- **27a~27d** 로그인 복구. `osascript`의 Terminal 제어 권한(Automation) 창은 사람이
  눌러야 한다. 허용해 주면 나머지 흐름은 이어서 확인할 수 있다.
- **8·8a·19·22a·37**의 `.app` 번들 기준 반복.

#### 환경 원복 확인

`config.toml` 원본과 무차이, `~/.codex/auth.json` 원본과 동일, autostart `enabled`,
`language=ko`, `native_notifications=true`, `popup.position=auto`, `welcomed=True`.
커서 위치도 매번 원래 자리로 되돌렸다.

### 2026-09-13 2차 — 남은 항목 전수 시도

권한을 넓혀 받아 되돌릴 수 있는 범위 전부를 직접 조작했다.

#### 새로 찾아 고친 결함 1건

- **알림 연동 설정 창이 한 번 닫히면 다시 열리지 않음.** `IntegrationDialog`이
  `WA_DeleteOnClose`라 닫히면 C++ 객체가 지워지는데
  ([integration.py:65](src/tokentray/ui/integration.py#L65)), Controller는 죽은 래퍼를
  계속 들고 있었다. 다음 호출의 `existing.isVisible()`이
  `RuntimeError: Internal C++ object (IntegrationDialog) already deleted`로 터지고,
  예외가 슬롯 안에서 사라져 **메뉴를 눌러도 아무 일도 일어나지 않았다.** 프로세스가
  끝날 때까지 복구되지 않는다. `destroyed` 신호로 참조를 놓도록 수정했고, Esc로 닫고
  재오픈하기를 3회 반복해 매번 열리고 `RuntimeError` 0건임을 확인했다.
  플랫폼 무관한 결함이다.

#### 추가 통과

| 항목 | 근거 |
|---|---|
| 4 | 툴팁 `tokentray - CC 5h 61% 7d 95% \| CX 7d 100%` — `tokentray`로 시작하고 기간별 잔량 포함. 축약형이 툴팁에만 쓰인다는 17a 조건도 만족. |
| 14a | 창을 최소 너비 440px로 줄여도 안내문·체크박스·콤보·입력란·`알림 이용 안내` 박스·버튼이 잘리거나 겹치지 않음. 외부 알림을 끈 상태에서도 안내가 계속 표시됨. |
| 14b | URL 입력이 `•`로 마스킹되고, 서비스를 바꾸면 필드가 비워짐. 저장 시 `enabled=true, kind=generic, configured=true`가 즉시 기록되고 URL은 설정 파일이 아니라 키링으로 감. |
| 14c | Slack `https://hooks.slack.com/services/T…/B…/…`, Discord `https://discord.com/api/webhooks/<id>/<token>`, ntfy `https://ntfy.sh/<topic>` — `<id>`·`<token>`·`<topic>`이 HTML 태그로 먹히지 않고 그대로 보임. Generic에는 형식 안내 없음. |
| 15 | 시스템 테마를 라이트→다크로 바꾸고 각각 재실행해 팝업과 상세 창이 해당 테마로 그려지는 것을 캡처로 확인. 원래 값(라이트)으로 복원. |
| 24 | Wi-Fi를 내린 뒤 강제 조회: 마지막 값 58%/94%/100%를 유지하고 `소스: 오래된 데이터 - ConnectError`로 표시. 숫자를 비우지 않음. 즉시 복구. |
| 25 | Codex 토큰을 만료된 것으로 바꾸자 `Codex 로그인이 만료됐어요 — 터미널에서 'codex' 실행` + `소스: HTTP 401`. 강제 조회 3회 중 첫 회에만 카드 1장, 이후 0장 — 매 조회 반복 없음. 원본 복구 확인. |
| 27d | Codex CLI가 없는 상태의 만료 패널에 `설치 안내`·`다시 확인` 버튼과 `CLI를 찾지 못했어요. 설치했다면 PATH를 확인하거나…` 안내가 표시됨. 터미널 실행이 실패했을 때 `LaunchError: terminal_failed`로 떨어져 명령 복사 경로로 가는 것도 확인. |
| 32 | 키체인 읽기가 거부되는 상황(= `security`가 비정상 종료)에서 앱이 죽지 않고 Claude만 `Claude Code 미로그인`으로 표시되고 Codex는 정상 동작. |
| 19 (번들) | `.app/Contents/MacOS/tokentray status`가 데이터와 pid를 출력. |
| 37a (번들) | 재빌드한 번들도 `app bundle <경로> (ad-hoc)`, `codesign`상 `Signature=adhoc`, `TeamIdentifier=not set`. |

#### 코드로 확인한 구조적 사실 (실기 재현 불가)

- **32·33·36의 전제가 이 구현과 다르다.** Claude 자격 증명은
  [claude.py:139-156](src/tokentray/providers/claude.py#L139-L156)에서 `/usr/bin/security`
  외부 명령으로 읽는다. 키체인 ACL은 tokentray 번들이 아니라 `security`를 기준으로
  평가되므로, **ad-hoc 서명이 빌드마다 바뀌어도 권한이 무효화되지 않는다(36번 전제 무효).**
  실제로 새로 빌드한 번들을 실행해도 프롬프트가 뜨지 않았다.
- **33번이 우려하는 반복 프롬프트를 막는 장치가 없다.**
  [base.py:100-103](src/tokentray/providers/base.py#L100-L103)에서 `credentials()`가
  None이면 백오프를 남기지 않고 곧바로 `NOT_CONFIGURED`로 끝난다. 자격 증명 읽기가
  거부된 상태라면 `security`가 **폴링마다(기본 120초) 다시 호출**된다. 이 기기는 ACL이
  이미 허용이라 프롬프트를 재현할 수 없었지만, 코드 경로상 방어는 없다.

#### 34번 (부분)

`.app` 아이콘 원본이 동심 2중링인 것은 확인했다(`packaging/resources/tokentray.icns`,
16~1024px 10종 수록). Finder·Gatekeeper 대화 상자 실물 확인은 화면이 잠들어 중단됐고,
Gatekeeper 쪽은 격리 속성이 붙은 배포 파일이 필요하다.

#### 막힌 지점

- **27a~27c** — `osascript`가 Terminal을 띄우려는 순간 macOS Automation 동의 창이
  떴다. 동의 창은 대신 눌러선 안 되고 합성 클릭도 막혀 있어 여기서 멈췄다. 창의
  주체는 tokentray가 아니라 **Visual Studio Code**였다 — 내가 셸에서 호출했기 때문이고,
  실제 사용자가 `.app`에서 누르면 tokentray 이름으로 뜬다. 권한을 허용해 주면 5분 대기,
  취소, 재조회까지 이어서 확인할 수 있다.
- **8·8a·22a의 번들 판정** — 화면이 잠들어 OS 배너 유무를 눈으로 확인하지 못했다.
  이 셋은 미서명 번들에서 배너가 실제로 뜨는지가 핵심이라 화면이 필요하다.
- **13** 외부 모니터, **21** 실제 로그아웃, **35** 태그된 릴리스 — 장비·환경이 없어
  내가 만들 수 없다.
- **23** 메뉴에서 종료 — 앱을 내리는 동작이라 검증 마지막에 해야 해서 남겼다.

#### 환경 원복 재확인

`config.toml` 원본과 무차이, `~/.codex/auth.json` 원본과 동일, 키링의 `webhook.url`
임시 항목 삭제, 시스템 테마 라이트(원래 값), Wi-Fi 켜짐, autostart `enabled`,
체크아웃 실행분으로 복귀. 검증용 `.app`은 스크래치패드에만 있고 저장소 `dist`는
건드리지 않았다.

### 2026-09-13 3차 — Automation 허용 후 전수 완료

사용자가 Automation 동의 창을 허용해 줘서 막혀 있던 경로를 모두 통과했다.

#### 로그인 복구 (27a~27d)

실제 자격 증명은 건드리지 않았다. `HOME`·`CLAUDE_CONFIG_DIR`·`CODEX_HOME`을 임시
디렉터리로 돌린 **격리 인스턴스**를 띄우고 만료된 자격 증명 파일을 넣어 시험했다.

| 항목 | 근거 |
|---|---|
| 27a | 만료된 Claude 영역에 `Log in`·`Check again`과 `Sign in to Claude Code in the terminal that opens.`가 표시됨. 누르니 Terminal이 열리고 `/usr/bin/env CLAUDE_CONFIG_DIR=… CODEX_HOME=… /Users/…/claude auth login`이 실제로 실행되어 OAuth URL까지 출력됐다. **환경 변수 인계가 의도대로 동작**해 격리 디렉터리로만 쓰인다. 같은 버튼이 자체 카드에도 붙는다. |
| 27b | 대기 중 `Waiting for login…` 표시. 자격 증명 파일을 바꾸자 0.2초 안에 `Credentials changed; checking usage…`로 전환되고 재조회에 들어갔다. 5분 타임아웃은 [login_recovery.py:107](src/tokentray/login_recovery.py#L107)의 `deadline = time.monotonic() + 300`으로 확인했고 실시간 대기는 생략했다. |
| 27c | `Stop waiting`을 누르니 버튼이 `Log in`·`Check again`으로 돌아가고, **Terminal 창 수가 3으로 그대로**였으며 로그인 세션도 살아 있었다. |
| 27d | Codex는 CLI가 없어 `Installation guide`·`Check again`과 PATH 안내가 표시됨. Automation 권한이 없던 1차 시도에서는 `LaunchError: terminal_failed`로 떨어져 명령 복사 경로로 가는 것도 확인했다. |

Automation 동의 창의 주체는 **Visual Studio Code**였다. 내가 셸에서 호출했기 때문이고,
사용자가 `.app`에서 누르면 tokentray 이름으로 뜬다.

#### 번들에서만 판정 가능한 항목

| 항목 | 근거 |
|---|---|
| 8a | **ad-hoc 서명 `.app`에서 OS 배너가 실제로 떴다.** `tokentray · 알림 테스트 / Claude Code: 5시간 50% 남음, 7일 93% 남음 / Codex: 7일 100% 남음`. 자체 카드 2장과 동시에 표시된다. **9월 10일 기록의 "미서명이라 배너가 없는 것이 정상"이라는 전제는 ad-hoc 번들에는 해당하지 않는다.** |
| 22a | `native_notifications=false`에서 알림 시도 기록 0건·배너 0개·자체 카드만. `true`로 되돌리면 시도 기록 +1·배너 1개. 둘 다 재시작 없이 즉시 적용. |
| 23 | 메뉴의 종료로 프로세스가 사라지고 메뉴 막대 아이콘도 없어지며 `doctor`가 `running no`를 출력. |
| 34 (앞절반) | Finder에서 `.app`이 기본 아이콘이 아닌 **동심 2중링 아이콘**으로 표시됨. `CFBundleIconFile=tokentray.icns`, 16~1024px 10종 수록. |
| 35 (기전) | 사본에 `com.apple.quarantine`을 직접 붙이니 `spctl`이 `rejected`로 판정하고 실행 시 **"'tokentray'을(를) 열지 않음"** 대화 상자가 떴다. `xattr -d com.apple.quarantine` 후에는 대화 상자 없이 실행됐다. |

#### 체크리스트 문구와 실제가 어긋나는 곳

- **34번** — Gatekeeper 차단 대화 상자에는 **앱 아이콘이 나오지 않는다.** 시스템 경고
  아이콘을 쓴다. "Gatekeeper 대화 상자에서 이중 링 아이콘이 표시됩니다"는 현재 macOS
  동작과 맞지 않는다. Finder 쪽은 맞다.
- **35번** — "로컬 빌드에는 격리 속성이 없어 이 항목을 검증할 수 없습니다"라고 되어
  있지만, 사본에 속성을 직접 붙이면 차단과 해제를 모두 재현할 수 있다. 다만 진짜
  배포 파일의 다운로드 경로는 여전히 미검증이다.
- **8a번** — macOS 미서명 빌드에서 배너가 없을 것이라는 전제가 ad-hoc 번들에서는
  성립하지 않는다.

#### 그 밖에 관찰한 것

- OS 배너와 자체 카드가 **둘 다 화면 오른쪽 위를 차지해 서로 가린다.** macOS의 `auto`가
  위쪽 배치인데 Notification Centre도 같은 자리라 첫 카드가 배너에 덮인다.
- 소켓 경로가 길면(`QLocalServer::listen: Name error`) 앱이 이를 **"다른 tokentray가
  잠금을 잡고 있지만 응답이 없습니다"로 오인**하고 종료한다. 실제 원인은 유닉스 소켓
  경로 길이 제한(약 104자)이라 안내가 오도한다. 일반 사용 경로에서는 나지 않는다.
- `claude auth login`은 시작할 때 기존 `.credentials.json`을 지운다. 로그인을 중간에
  그만두면 원래 자격 증명이 사라지므로, 사용자가 취소했을 때의 상태를 안내에
  반영하면 좋겠다.

#### 환경 원복 재확인

`config.toml` 원본과 무차이, `~/.codex/auth.json` 동일, `~/.claude/.credentials.json`은
원래대로 없음(키체인 사용), 키링 임시 항목 삭제, 시스템 테마 라이트, Wi-Fi 켜짐,
autostart `enabled`, `/tmp/tt-iso`와 격리 테스트 사본 삭제, 체크아웃 실행분 복귀.
Claude 49%/93%, Codex 100% 정상 조회 확인.

### 2026-09-13 4차 — 장비 없이 갈 수 있는 데까지

| 항목 | 결과 | 근거 |
|---|---|---|
| 13 | 자동 테스트로 대체 | 외부 모니터가 없어 실기는 못 했지만, `_area()`가 트레이 아이콘이 있는 화면을 고르는 로직을 `tests/test_ui.py`에 고정했다. `screenAt`을 가짜 두 번째 화면으로 바꿔 카드가 그 화면 안에 놓이는지 단언하며, 앵커를 무시하도록 되돌리면 실패하는 것까지 확인했다. 실기 확인에 남는 것은 "Qt가 올바른 화면을 보고하는가" 뿐이다. |
| 21 | 통과 (launchd 경유) | 로그아웃 대신 `launchctl kickstart -k gui/<uid>/io.github.messy-snail.tokentray`로 **launchd가 로그인 때 하는 것과 같은 방식**으로 띄웠다. 앱이 뜨고(트레이 아이콘 `(1056, 4)`), 콘솔 창은 생기지 않았으며, `welcomed`를 일부러 지웠는데도 시작 안내가 뜨지 않았다. [app.py:205](src/tokentray/app.py#L205)가 `--autostart`일 때 안내를 건너뛰되 `welcomed`를 남기지 않아 다음 수동 실행에서는 정상적으로 보인다. 남는 것은 실제 로그인 이벤트뿐이다. |
| 33 | **결함 확인 → 수정됨** | 자격 증명 읽기 실패에 백오프가 없어 폴링마다, 로그인 복구의 2초 확인마다 `security`를 다시 불렀고, 거부가 미로그인으로 보여 사용자를 그 반복으로 보냈다. 아래 5차에서 수정했다. |

### 2026-09-13 5차 — 33번 수정과 실측

`security` 종료 코드 44(항목 없음)만 미로그인으로 두고, 그 밖의 실패·시간 초과는
`CredentialsUnreadable`로 올린다. 한 번 거부되면 자동 폴링과 2초 확인은 키체인을 다시
읽지 않고, 사용자가 누른 새로고침·다시 확인·로그인만 다시 읽는다. 새 상태
`UNREADABLE`로 "키체인에서 읽지 못했어요"를 표시하고 로그인 복구 흐름으로 보내지 않는다.

| 확인 | 결과 |
|---|---|
| 자동 폴링 (스텁, 폴링 30초) | 100초 동안 `security` 호출 **1회** (수정 전 30초마다 1회) |
| `tokentray refresh` (= 패널 새로고침과 같은 `refresh(force=True)`) | 호출 1 → 2 |
| 이후 30초 무입력 | 2 → 2 |
| 표시 문구 | "키체인에서 Claude Code 인증 정보를 읽지 못했어요 — macOS가 물으면 허용한 뒤 새로고침". 미로그인이 아니다 |
| 거부 상태 UI | 오프스크린 렌더에서 복구 문구, `다시 확인` 버튼, 패널 표시, 1회 경고 토스트(로그인 요구 없음), 트레이 툴팁 모두 예외 없이 동작 |
| 회귀 테스트 | 가드를 제거하면 `test_polls_do_not_ask_again`, `test_repeated_probes_ask_once` 실패. 복원 후 전부 통과 |

한계: 실측 도중 **화면이 잠겨** 있어(`CGSSessionScreenIsLocked`) AX가 창 목록을 받지 못했다.
정상 앱에서도 같았다. 그래서 패널의 `다시 확인` 버튼을 실제로 눌러 보는 것과 경고 토스트를
화면으로 보는 것은 하지 못했다. `다시 확인`이 다시 읽는다는 점은 단위 테스트
(`test_only_the_users_own_actions_read_interactively`, `test_an_explicit_check_asks_again`)로
확인했다.

관찰: 거부 상태에서 소스 줄이 `소스: keychain`으로 원문 그대로 나오고, 트레이 툴팁이 긴
안내 문구 전체를 담는다. 동작에는 문제가 없다.

### 2026-09-13 6차 — 최종 실기 (HEAD `8a03a2a` + 시작 시 경합 수정)

`c0f7811`(IPC)·`8a03a2a`(새로고침 판정)이 들어간 HEAD로 실제 앱을 launchd 경유로
재시작했다. 키체인 쪽은 `security`를 PATH 스텁으로 바꾸고 `HOME`·`CODEX_HOME`을 분리한
격리 인스턴스로 확인했다. 실제 자격 증명·설정은 건드리지 않았다.

#### 새로 찾아 고친 결함 1건

- **시작할 때 키체인 허용 창이 두 번 뜰 수 있음 (32·33)** — 앱이 뜨는 순간 첫 폴링
  ([app.py:203](src/tokentray/app.py#L203))과 로그인 복구 확인
  ([app.py:185](src/tokentray/app.py#L185))이 서로 다른 스레드에서 동시에
  `credentials()`를 부른다. 둘 다 `_keychain_refused`를 먼저 확인하고 읽기에 들어가서,
  거부가 기록되기 전에 `security`가 두 번 호출됐다. 격리 인스턴스 4개 모두 시작 1초 안에
  2회였고, 호출한 프로세스는 같았다(ppid 기록). 두 스레드 재현 스크립트는 5/5.
  실제로 거부하면 첫 호출이 프롬프트에 묶여 있는 동안 둘째가 들어오므로 사실상 항상
  겹친다. [claude.py](src/tokentray/providers/claude.py)에서 확인과 읽기를 모듈 락으로
  묶었다. 회귀 테스트 `TestReadsThatStartTogether::test_they_ask_once`는 수정 전
  `2 == 1`로 실패하고 수정 후 통과한다. 수정 코드로 띄운 인스턴스는 시작 1회, 이후
  40초 동안 0회.

#### 통과

| 항목 | 근거 |
|---|---|
| 18 | 두 번째 실행이 `tokentray가 이미 실행 중입니다 (pid N) - 상세 패널을 엽니다.`를 출력. |
| 19 | `status`가 데이터와 `(from the running app, pid N)`을 출력. |
| 27f | 실제 앱 패널 새로고침: `갱신 중…` 오버레이 → `갱신 완료`. |
| 27f (한 서비스) | Claude 미로그인(스텁 종료 코드 44)·Codex 로그인 상태: `갱신 중…` → `갱신 완료`. `8a03a2a` 이전에는 `갱신 실패`로 나오던 조합이다. |
| 27f (거부) | 키체인 거부 상태: `갱신 중…` → `갱신 실패`, 키체인 읽기 +1. |
| 32 | 거부 상태 패널에 `키체인에서 Claude Code 인증 정보를 읽지 못했어요 — …`와 `다시 확인` 버튼만 표시. 미로그인이나 `로그인 바로가기`로 보내지 않는다. |
| 33 | 자동 폴링(30초 설정) 134초 동안 읽기 0회. `tokentray refresh` +1, 패널 새로고침 +1, `다시 확인` 2회 +2. 경고 토스트는 시작 때 1회(`notified_status.claude=unreadable`), 로그인 요구 없음, 8초 뒤 소멸. |
| IPC (`c0f7811`) | 맥 `status` 왕복 0.4~0.6ms로 수정 전(0.3~0.8ms)과 같다. `waitForDisconnected`가 GUI 스레드를 붙잡지 않는다. |

`uv run pytest -q` 전체, `uv run ruff check .` 통과.

#### 관찰

- 거부 상태의 Claude 영역에 비슷한 안내가 두 줄 나온다. 상태 문구는 "…허용한 뒤
  새로고침", 복구 문구는 "…허용한 뒤 다시 확인해 주세요"라 서로 다른 버튼을 가리킨다.
- Bartender가 체크아웃 실행분 아이콘을 모두 `(-3952, 4)`로 숨긴 상태에서는 패널이
  화면 오른쪽 아래에 붙는다. 패널(`Qt.Popup`)은 AX 창 목록에 잡히지 않고, 높이가
  내용에 따라 달라 버튼 좌표가 매번 바뀐다.
- `python -m tokentray doctor`는 doctor가 아니라 GUI 진입점으로 가서 두 번째 실행
  처리(패널 열기)를 한다. CLI는 `tokentray doctor`로 불러야 한다.
- CHANGELOG `Fixed`에 알림 연동 설정 창 재오픈 수정이 두 번 적혀 있다.

### 2026-09-13~14 7차 — 최종 코드로 체크리스트 전수 재실행

락 수정이 들어간 작업 트리로 맥 체크리스트 전체를 처음부터 다시 돌렸다. 실제 앱(체크아웃)과
스크래치패드에 새로 빌드한 ad-hoc `.app`(스모크 통과, `Signature=adhoc`)을 모두 썼다.
설정·상태·자동 시작 plist·`~/.codex/auth.json`을 백업하고 매 단계 원복을 대조했다.

#### 새로 찾은 결함 (미수정)

- **10 — 다른 앱이 활성일 때 호버가 카운트다운을 멈추지 않는다.** tokentray가 맨 앞일 때는
  올린 카드가 24초까지 남았지만, VS Code가 맨 앞이면 커서를 카드 위에 고정해도 8초에 사라졌다.
  [popup.py:306-317](src/tokentray/ui/popup.py#L306-L317)이 `enterEvent`/`leaveEvent`에만
  의존하는데, 비활성 앱의 창에는 macOS가 Enter를 보내지 않는다. 카드는 보통 다른 앱을 쓰는
  중에 뜨므로 일반적인 경우에 실패한다.
- **12 — 아래쪽 배치에서 아래 카드의 X가 위 카드 창의 그림자 여백에 덮인다.** 위 카드 창은
  y=571~752, 아래 카드의 X 중심은 y≈743이다. 카드 창은 여백을 포함한 전체에서 클릭을 받고
  ([popup.py:323-327](src/tokentray/ui/popup.py#L323-L327)) 카드 영역 검사가 없어, X를 누르면
  **위 카드가 닫히고 상세 창이 열렸다.** 겹치지 않는 본문을 누르면 재배치는 정상이다
  (남은 카드가 아래 슬롯 y=760으로 이동). 위쪽 배치(auto)는 정상.
- **14b — 저장된 서비스로 되돌아가면 입력한 URL이 지워지지 않는다.** Slack URL을 입력한 뒤
  원래 저장된 ntfy로 바꾸면 입력값이 남았다(캡처 67). 다른 서비스로 바꿀 때만 지우는 조건
  ([integration.py:211-219](src/tokentray/ui/integration.py#L211-L219)) 때문이다. ntfy는 모든
  http(s) URL을 받으므로 그대로 저장하면 Slack 주소가 ntfy 목적지로 저장된다.

#### 체크리스트 문구와 실제가 다른 곳

- **3a** — `설정하지 않음`(미로그인)은 링 자리를 유지하지만, `enabled=false`로 **끈** 서비스는
  [providers/\_\_init\_\_.py:27](src/tokentray/providers/__init__.py#L27)에서 목록에서 빠져 남은
  서비스가 바깥 링 전체를 쓴다. "끄거나 설정하지 않아도 위치 유지"와 맞지 않는다.
- **35** — 체크리스트의 `xattr -d com.apple.quarantine`으로는 사본의 속성이 남아 실행되지
  않았다. `xattr -dr`(재귀)로 지운 뒤에야 격리 사본이 자기 경로에서 실행됐다.

#### 통과

| 항목 | 근거 |
|---|---|
| 1 | 수동 재실행 후 메뉴 막대 아이콘 0.51초. |
| 2·3·3b | 오프스크린 픽셀 검사. 51%→green, 50.4%·21%→orange, 20.4%→red. 바깥 80%는 9시까지, 안쪽 30%는 3시를 지남, 12시부터 시계방향. Codex 0%는 12시에 짧은 빨간 표시, 3시는 트랙. |
| 3a (미로그인) | 안쪽 밴드가 트랙으로 남고 바깥 링 불변. |
| 4 | 툴팁 `tokentray - CC 5h 84% 7d 89% \| CX 7d 98%`. |
| 5 | 메뉴 전 항목 한국어. |
| 7 | 상세 창 바깥 클릭으로 닫힘. |
| 8 | 조회 중 `알림용 사용량 조회 중…`(비활성) 후 복귀, 카드 2장이 약 8초 뒤 소멸. 동시에 겹친 요청은 한 번으로 합쳐지고, 끝난 뒤 다시 요청하면 새로 뜬다. |
| 8a (번들) | 번들에서 실행 확인(`bundle=…/tokentray.app`) 후 알림 테스트 시 **OS 배너** `tokentray · 알림 테스트`가 자체 카드와 함께 표시, 시도 기록 +1. |
| 9 | TextEdit에 120자 입력 중 카드 표시. 입력 120자 정확히 일치, 포커스는 TextEdit 유지. |
| 11 | 카드 클릭으로 상세 창 열림. 앱이 비활성이면 첫 클릭은 활성화에 쓰이고 두 번째 클릭에 열린다. |
| 12 (auto) | 위 카드를 닫으면 아래 카드가 y=33으로 이동. |
| 14 | 일시정지 중 강제 알림 0장, 메뉴 `알림 다시 켜기`, 재개 후 같은 조건에서 카드 2장. |
| 14a | 최소 너비 440에서 안내·입력란·버튼 잘림 없음. 테스트 실패(`설정을 적용하지 못했습니다: 테스트 실패: ConnectError`)·성공(`테스트 알림을 보냈습니다.`) 후에도 `알림 이용 안내` 유지. |
| 14b (나머지) | URL `•` 마스킹, 다른 서비스로 바꾸면 비워짐. 저장 시 `enabled=true, kind=generic, configured=true, url=""`, URL은 키링에만. 재시작 없이 한글 사용량 알림이 웹훅으로 도착(`5시간: 77% 남음` 등). |
| 14c | 네 서비스 모두 번호 매긴 설정 방법과 링크. `<topic>`·`T…/B…/…`·`<id>/<token>` 그대로 표시, Generic은 형식 안내 없음. |
| 14d | `native_notifications=false`에서 auto=위(y=33), bottom-right=아래(카드 아래끝 941), top-right=위. 재시작 없음. |
| 15 | 라이트로 바꿔 재시작 시 카드·상세 창 밝게, 다크로 되돌려 재시작 시 어둡게. 원래 값(다크) 복원. |
| 16·17·17a·17b | English 전환 시 메뉴·패널·카드 전부 영문(`5 hours`·`remaining`·`Refills in`·`Burns out in`·`Pace`), 제목 `Claude Code (Max)`·`Codex (Pro Lite)`, 한국어 복귀 정상. |
| 18·19 | 두 번째 실행 안내와 pid, `status`의 `(from the running app, pid N)`. 번들 CLI `status`도 실행 중 앱 데이터 출력. |
| 22 | `autostart disable`로 plist 삭제, `enable`로 복구. |
| 22a (번들) | `false`에서 시도 기록 +0·배너 없음, `true`로 되돌리면 +1·배너 표시. 재시작 없음. |
| 23·23a | 메뉴 종료 2.6초 후 프로세스·아이콘 사라지고 `running no`. 실행 중 `state reset --welcome` 후 강제 저장을 거쳐도 `welcomed`가 되살아나지 않고 재실행 시 시작 안내 표시. |
| 24 | Wi-Fi 끈 상태에서 마지막 값 유지, `오래된 데이터 - ConnectError`. 켜자 4초 뒤 실시간 복귀. |
| 25 | Codex 만료 토큰: `Codex 로그인이 만료됐어요 — 터미널에서 'codex' 실행` + `HTTP 401`, 강제 조회 3회에 알림 1회. 원본 복구 확인. |
| 26 | `auth.json` 제거 시 Codex만 `Codex 미로그인`, Claude 실시간. 원본 복구 확인. |
| 27·37a | 체크아웃 doctor `running yes (pid N)`·`app bundle none`, 번들 doctor `app bundle <경로> (ad-hoc)`, 양쪽 Notification Centre 안내. |
| 27a (버튼) | 만료 상태의 상세 창과 자동 알림 카드 모두 Claude에 `로그인 바로가기`·`다시 확인`. |
| 27d | CLI가 없는 Codex에 `설치 안내`·`다시 확인`과 PATH 안내. |
| 27e | 격리 HOME에서 `setup` pty 실행: 언어 먼저, 서비스별 CLI·인증 표, 다시 확인·설치 안내(URL만 열기)·로그인(인증 URL 대기)·비활성화·건너뛰기·자동 시작 질문. 수동 토큰은 실제 키링 저장까지 확인 후 삭제. |
| 27f | 실제 앱 `갱신 중…`→`갱신 완료`, Codex만 로그인한 경우 `갱신 완료`, 키체인 거부 상태 `갱신 실패`. |
| 32·33 | (6차) 거부 안내와 `다시 확인`만 표시, 자동 폴링 중 재조회 0회, 시작 시 읽기 1회. |
| 34 | Finder에서 `.app`이 이중 링 아이콘. |
| 35 (기전) | 격리 속성 사본 실행 시 `'tokentray'을(를) 열지 않음` 대화 상자(휴지통으로 이동/완료). `xattr -dr` 후 격리 사본이 자기 경로에서 실행. |
| 36 | 새 번들 실행 시 SecurityAgent 없음, Claude·Codex 모두 `ok`. |
| 37 (안내) | Bartender 실행 중 시작 안내에 메뉴 막대 관리 앱 숨김 해제 문구 포함. |

#### 못 한 것

- **27a(터미널 실행)·27b·27c** — 합성 클릭으로 상세 창 버튼을 안정적으로 누르지 못했다(패널이
  AX에 늦게 잡히거나 클릭이 VS Code로 감). 버튼·문구·상태 전이 코드는 확인했고, 3차에서 실제
  CLI로 통과한 경로다. 재확인은 사람이 버튼을 눌러야 한다.
- **37(숨김 해제)** — 사용자의 Bartender 설정 변경이 필요하다.
- **13** — 외부 모니터 없음(로직은 테스트로 고정).

#### 관찰

- 검증 중 알림 테스트를 연 인스턴스를 강제 종료하면 launchd `KeepAlive`가 즉시 다시 띄운다.
  번들을 확인하려면 체크아웃 앱을 `stop`(정상 종료)으로 내려야 한다.
- 번들 배너 본문이 `Claude Code: 이전 데이터: 5시간 70% 남음…`처럼 콜론이 겹친다.
- `setup`의 서비스 선택에서 목록에 없는 입력은 경고 없이 건너뛰기로 처리된다.
- CHANGELOG `Fixed`에 알림 연동 설정 창 재오픈 수정이 두 번 적혀 있다.

#### 원복 확인

`config.toml` 백업과 동일, `~/.codex/auth.json` 백업과 동일, 키링 임시 항목(`webhook.url`,
수동 토큰) 삭제, 시스템 테마 다크(원래 값), Wi-Fi 켜짐, 자동 시작 `enabled`, 실제 앱은 launchd로
체크아웃 실행분 복귀, 테스트용 터미널 창 닫힘. 검증용 `.app`과 격리 HOME은 스크래치패드에만 있다.

### 남은 것

1. **35** — 태그된 릴리스를 브라우저로 내려받아 격리 속성이 붙는지. 차단과 해제
   기전은 사본으로 재현했다. 배포 후 확인한다.

정리된 것: **13**은 외부 모니터가 없어 사용자 판단으로 생략했다(로직은 테스트로 고정).
**21**은 사용자가 실제 로그아웃·재로그인으로 확인했다(2026-09-13).
**33**은 6차에서 화면으로 확인했고, 시작 시 이중 호출도 함께 수정했다.

---

## 2026-09-10 실행 (기준 `5efdc2f`)

전체 결과: **미완료, 실제 화면에서 문제 2건 확인**. 화면 접근 권한 문제를 해결한 뒤
클릭·화면 캡처로 검증했다. 코드 변경은 없으며, 아래 결과는 전체 체크리스트 통과를 뜻하지 않는다.
아래 9·16번은 위 갱신에서 수정됐고, 재검증은 아직이다.

## 확인된 문제

### 1. 다른 앱이 활성화되면 자체 토스트가 보이지 않는다

- 체크아웃과 현재 소스로 빌드한 `.app`에서 모두 관찰했다.
- tokentray를 활성화한 상태에서는 테스트 토스트 두 개가 표시된다.
- 표시 직후 TextEdit를 활성화하면 약 8초의 표시 시간이 지나기 전에 토스트가 숨겨진다.
- TextEdit에서 입력하는 동안 IPC `test-alert`를 보내면 성공 응답은 오지만 자체 토스트가 보이지 않는다.
- 입력한 `1234567890` 12회, 총 120자는 정확히 유지됐다. 하지만 토스트가 보이지 않아
  9번의 “입력 중 팝업 표시와 포커스 유지” 시나리오는 통과하지 못했다.
- 새 `.app`에서는 같은 상황에서 OS 배너는 도착했다. 자체 토스트와 OS 배너를 구분해야 한다.

증거: [활성 상태](</tmp/tokentray-mac-qa-20260910/screens/top-foreground.png>),
[TextEdit 전환 직후](</tmp/tokentray-mac-qa-20260910/screens/top-background.png>),
[입력 중](</tmp/tokentray-mac-qa-20260910/screens/focus-during.png>),
[번들에서 OS 배너만 표시](</tmp/tokentray-mac-qa-20260910/screens/bundle-background-toasts.png>).
이 기록은 관찰 결과이며, 원인 코드나 수정 방법은 아직 확정하지 않았다.

### 2. 언어 변경 직후 상세 창에 한국어와 영어가 섞인다

1. 한국어로 실행 중인 체크아웃에서 트레이 메뉴의 `언어 → English`를 선택한다.
2. 설정의 `language='en'`을 확인하고 상세 창을 다시 연다.
3. 버튼은 `Refresh now`로 바뀌지만 기간·사용량 설명은 `5시간`, `남음`, `리셋까지`,
   `소진 예상`, `속도`, `실시간`으로 남는다. 다음 테스트 토스트는 영어로 바뀐다.

증거: [언어 변경 후 상세 창](</tmp/tokentray-mac-qa-20260910/screens/panel-en-final.png>),
[영어 버튼과 한글 본문](</tmp/tokentray-mac-qa-20260910/screens/toast-en.png>).
다음 폴링 후 자동으로 해소되는지까지는 이번에 확인하지 않았다.

## 항목별 결과

| 항목 | 결과 | 직접 확인한 내용과 한계 |
|---|---|---|
| 1 | 미검증(부분 확인) | 체크아웃 아이콘을 실제로 봤다. 최초 실행부터 10초 이내라는 시간 조건은 측정하지 않았다. |
| 2·3 | 미검증(부분 확인) | 실제 메뉴 막대에서 주황색 이중 링을 구분했다. 당시 두 서비스의 최소 잔여량은 주황 구간이었다. 모든 색상 경계 및 링 감소 방향은 별도 검증이 필요하다. |
| 3a·3b·4 | 미검증 | 서비스 비활성화·0% 입력·실제 툴팁 호버 조건을 완성하지 않았다. |
| 5 | 통과 | 실제 아이콘 클릭으로 한국어 메뉴가 열리는 것을 확인했다. |
| 6 | 해당 없음 | 체크리스트가 Windows/Linux 왼쪽 클릭 동작으로 정의한다. |
| 7 | 미검증(부분 확인) | 다른 앱 활성화 시 상세 창이 닫히는 것을 확인했다. 외부 클릭만을 분리한 검증은 하지 않았다. |
| 8 | 미검증(부분 확인) | 두 서비스 이름·색상 띠·게이지·한글 본문을 실제로 확인했다. 화면에는 서비스별 로고가 보이며, 체크리스트가 요구하는 “tokentray 링”과 문서 정합성 확인이 필요하다. 백그라운드 표시 문제도 있다. |
| 8a | 통과(현재 번들) | 현재 소스의 ad-hoc `.app`에서 제목이 `tokentray`인 실제 OS 배너를 확인했다. 미서명 빌드 전체에 일반화하지 않는다. |
| 9 | 실패 | 입력은 유지되지만 다른 앱 활성 상태에서 자체 토스트가 보이지 않는다. 위 재현 절차 참조. |
| 10 | 미검증(부분 확인) | 앱 활성 상태에서 약 8초 후 두 토스트가 사라짐을 확인했다. 호버 시 유지 조건과 페이드 중간 프레임은 충분히 검증하지 못했다. |
| 11 | 통과 | 실제 토스트 본문 클릭으로 상세 창이 열렸다. |
| 12 | 통과 | 두 토스트가 겹치지 않았다. bottom-right에서 아래 Claude 토스트를 닫자 Codex가 아래로 이동했다. |
| 13 | 미검증 | 내장 모니터 한 대만 연결되어 있다. |
| 15 | 통과 | OS 테마 변경 후 앱 재실행으로 밝은 상세 창·토스트를 확인했다. 어두운 테마의 상세 창·토스트도 확인했다. 밝은 토스트 일부는 OS 배너에 가려졌다. |
| 16 | 실패 | English 선택 직후 상세 창에 언어 혼합이 발생했다. |
| 17 | 미검증(부분 확인) | 본 한글 메뉴·상세·토스트에는 글리프 누락이 없었다. 상세 하단은 스크롤로 정상 확인했다. 모든 설정 창은 이번에 확인하지 않았다. |
| 17a | 미검증(부분 확인) | 한국어 상세에는 `5시간`·`7일`, 테스트 토스트에는 `5시간 세션`·`Codex 7일`이 보였다. 영문 기간 표기 전체 및 모델별 항목은 별도 점검이 필요하다. |
| 17b | 통과(현재 요금제) | 실제 상세 창에 `Claude Code (Max)`·`Codex (Pro Lite)`가 표시됐다. |
| 17c | 통과 | 동일 체크아웃 프로세스에서 auto의 상단 표시와 bottom-right → top-right 변경을 실제 화면으로 확인했다. 재시작하지 않았다. |
| 27·37a | 통과 | 체크아웃 doctor의 `app bundle none`과 새 번들의 경로·`ad-hoc`, 양쪽 Notification Centre 안내를 확인했다. |
| 32·33 | 미검증 | 새 번들 실행·강제 새로고침에서도 두 서비스가 `ok`였다. 키체인 Deny를 재현하지 못해 거부 후 생존·오류 표시 및 두 조회 주기의 재요청 억제는 검증하지 못했다. |
| 37 | 미검증(부분 확인) | Bartender 실행 중 체크아웃 링 아이콘은 실제로 보였다. 새 번들 아이콘은 화면에 없고 AX 좌표가 화면 밖이었다. Bartender 표시 조작을 시도했으나 숨김 해제 후 아이콘 출현 및 시작 안내는 확인하지 못했다. |
| 21·34·35·36 | 미검증 | 실제 로그아웃, Finder/Gatekeeper 아이콘, 배포 릴리스, 업데이트 후 키체인 프롬프트는 이번 결과에 포함하지 않는다. |

기존 대화에서 확인되었다고 보고된 22a·23a는 이번 실행에서 재검증하지 않았다.

## 환경과 측정 근거

- 소스: `5efdc2f`, tokentray 0.1.0, Python 3.12.4.
- OS: macOS 26.6.2 (25G83), 내장 Retina 한 대, 논리 1512×982, 배율 2.
- 기존 설정: `popup.position=auto`, `language=ko`, `poll_interval=120`,
  `native_notifications=true`, `webhook.enabled=false`, OS 다크 모드.
- 기존 `dist`는 9월 7일 빌드라 최신 doctor 줄이 없었다. 기존 빌드를 덮어쓰지 않고
  `/tmp/tokentray-mac-qa-20260910/dist/tokentray.app`에 검증용 빌드를 만들었다.
- 17c: AX 기준 top의 두 창은 대략 `(1103,33)`·`(1103,163)`, bottom은
  `(1103,777)`·`(1103,643)`, 각 창 크기 412×144였다. 창에는 그림자 여백이 포함된다.
- 12: 아래 창을 닫은 후 Codex가 `(1103,777)`로 이동했다.
- 10: 테스트 요청 뒤 약 1.01초에 창 두 개, 약 8.14초에 창 0개였다.
  이 측정만으로 페이드 곡선이나 정확한 8.000초 타이밍을 보장하지 않는다.

추가 화면: [한국어 메뉴](</tmp/tokentray-mac-qa-20260910/screens/menu.png>),
[기본 상단](</tmp/tokentray-mac-qa-20260910/screens/toast-auto.png>),
[하단](</tmp/tokentray-mac-qa-20260910/screens/bottom-0.6.png>),
[닫은 뒤 재배치](</tmp/tokentray-mac-qa-20260910/screens/reflow.png>),
[8초 후](</tmp/tokentray-mac-qa-20260910/screens/expiry-8.8.png>),
[밝은 상세 창](</tmp/tokentray-mac-qa-20260910/screens/light-panel-active.png>),
[밝은 토스트](</tmp/tokentray-mac-qa-20260910/screens/light-toasts.png>).
화면 파일은 로컬 임시 폴더에 있으며 저장소에 추가하지 않았다.

## 제어 경로와 복원

처음 CUA `getApp`이 `-10005 timeoutReached`로 반복 실패했다. 사용자 승인 후
`screencapture`와 AppleScript를 사용했다. 실행 프로세스는 VS Code 확장의 자식이었고,
화면 기록 권한이 없었다. 사용자 승인 뒤 VS Code 권한의 ON 상태와 실제 캡처 성공을
확인했다. 이후 체크아웃은 CUA로도 일부 조작할 수 있었다. 이 경과만으로 최초 CUA
시간 초과의 원인이 전부 VS Code 권한 때문이었다고 단정하지 않는다.

검증 후 popup.position=auto, language=ko, OS 다크 모드와 원래 체크아웃 실행으로
복원했다. 최종 PID는 60061이고 IPC 정상 응답을 확인했다. 폴링·native_notifications·
외부 알림 활성 설정은 기존 값 그대로다. 자동 실행 명령도 체크아웃의
`.venv/bin/python3 -m tokentray`로 확인했다. VS Code 화면 기록 권한은 사용자 승인대로
켜져 있다. 자격 증명과 키체인 접근 권한은 수정하거나 초기화하지 않았다.

입력 검증용 문서는 `/tmp/tokentray-mac-qa-20260910/focus.txt`에 저장하고 닫았다.
앱 실행·조회에 따른 정상적인 로그·캐시 갱신은 발생했다. 코드 수정, 커밋, 푸시는 하지 않았다.
